# Changelog

## 0.1.0 — 2026-05-15

- Initial release.
- `cursor-delegate` subagent.
- `/cursor` slash command.
- Default model: `composer-2`. Overridable via `--model` (slash) or `MODEL:` header (subagent).
